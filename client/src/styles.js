import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  header: {
    marginBottom: '15px',
    position: 'relative',
    borderRadius: '15px',
    backgroundColor: '#a6a6a6',
    color: 'white',
  }

})